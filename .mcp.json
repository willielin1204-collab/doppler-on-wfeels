{
  "mcpServers": {
    "onshape": {
      "command": "npx",
      "args": ["--yes", "onshape-mcp"],
      "env": {
        "ONSHAPE_ACCESS_KEY": "${ONSHAPE_ACCESS_KEY}",
        "ONSHAPE_SECRET_KEY": "${ONSHAPE_SECRET_KEY}"
      }
    }
  }
}
