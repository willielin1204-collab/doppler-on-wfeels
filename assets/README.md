# assets

首頁 Hero 背景照片放這裡：

- `hero-dow8.jpg` — DOW 8（University of Illinois / FARM）暴風追擊照片，
  由 `index.html` 的 `.hero-photo` 以 CSS background-image 載入。

若此檔不存在，首頁會優雅退回深色漸層背景（不會破版）。
建議尺寸：長邊 ≥ 1600px、JPEG/WebP，檔案 < 500KB 以兼顧畫質與載入速度。
